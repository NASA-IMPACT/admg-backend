{% comment %} TODO: Figure out a way to make this usable across repo so admin_ui doesn't have its own snippet. {% endcomment %}
{% load template_extras %}

<p class="{{ default_class }}">
  {{ preliminary_text }}
  {% for attribute in gcmd_path.path %}
    <span class="{{ gcmd_path_class }} {% if attribute.has_changed %} gcmd-changed {% endif %}"> {{attribute|keyvalue:path_attribute}} </span>
    {% if not forloop.last %} <span style="color: black;"> > </span> {% endif %}
  {% endfor %}
</p>
